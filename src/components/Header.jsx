import Logo from "./logo";
import Navigation from "./Navigation";
const Header = () => {
  return (
    <header id="main-header">
      <Logo />
      <Navigation />
    </header>
  );
};

export default Header;
